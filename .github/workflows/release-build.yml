name: Docker release build
on: [push]

jobs:
  build:
    name: docker build
    runs-on: ubuntu-latest
    steps:
    - name: checkout master
      uses: actions/checkout@master
      with:
        name: grayudu
    - name: build docker
      run: |
        docker build -t simpleweb:v1 .
